[package]
name = "hitplayer"
version = "0.1.4"
description = "A Windows media player and FFmpeg video utility."
authors = ["Rique"]
edition = "2021"
license-file = "../LICENSE"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
rfd = "0.15"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2", features = ["protocol-asset"] }
uuid = { version = "1", features = ["v4"] }
winreg = "0.55"
